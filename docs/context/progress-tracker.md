# Progress tracker

Last updated: 2026-09-25. Stage 01 and PR #4 are complete. The first Stage 02 workspace-admission slice is under review in PR #5 on `feat/workspace-admission`.

## Current scope

This Stage 02 slice covers verified-user workspace listing and operator-entitled creation with a persisted owner membership and scoped retry receipt. Project creation, invitations, role controls, archive/restore, and Google SSO belong to later reviewed slices. Atlas Outline remains a visual reference and was not copied or changed.

## Stage 02 workspace admission - PR #5

- A confirmed Supabase Auth subject resolves to an application profile. PostgreSQL stores operator-granted pilot entitlement, workspace, active owner membership, and a USER-scoped creation receipt. The web runtime can read but cannot change entitlement. Creation locks the profile and entitlement, enforces the owned-workspace quota in one transaction, and checks active ownership before receipt replay. A deferred database trigger rejects any second OWNER.
- No-store `/api/me` and `/api/workspaces` reads and a bounded same-origin create endpoint use verified server identity. The blank-canvas shell displays real memberships and owned quota. The create form keeps the same name and key after an uncertain response; a failed refresh hides stale workspace data. Project details remain empty until project data exists.
- The shell has independent sidebar controls: the workspace close icon is in the top-right corner of its panel, the context close icon is in the top-left corner, and the labels sit below them. Hidden panels have compact SVG reopen controls by the canvas. Both choices survive reloads within the current tab through sessionStorage; no panel preference is sent with requests. The main area waits for restoration before becoming visible, and temporary preference cookies from the earlier preview are expired. At the one-workspace pilot limit, the UI says the limit was reached. The disposable preview account owns test-workplace; the user chose to keep it. Recoverable archive belongs to a later, separately reviewed PR.
- The installed React types mark FormEvent deprecated for form submission. The workspace create handler now uses SubmitEvent. The two panel preference operations live in one feature-local helper used by both sidebars; there is no shared browser storage hook in this stage.
- A fresh disposable local Supabase stack applied both migrations; repeat migration reported no pending work. That test stack and its dedicated network were removed after verification, then restored with a fresh local database for user preview. The original local stack remained separate and running. The local guard grants only Auth schema USAGE and `auth.users(id)` REFERENCES to the migration role for the profile foreign key. Hosted migration grants and deployment are unverified. Operator entitlement provision UI is outside this slice.
- After review: Prisma validation, boundary check, ESLint, TypeScript, 17/17 unit and 14/14 database integration tests passed on Node 24.21.0. Integration coverage includes denied entitlement writes, active-access replay, owner uniqueness, member versus owned quota, and an operator revocation race. Focused workspace Chromium tests passed 6/6, including anonymous POST, uncertain retry, failed-refresh recovery and 390px overflow. The final full Chromium suite passed 16/16, including the prior auth flow, and the final Next production build passed.
- A confirmed local preview account with one unspent workspace allowance was created for user testing. The Stage 02 app is running at `http://127.0.0.1:3101` against this isolated stack. Browser sign-in, visible create control, zero owned workspaces, and sign-out passed. Its credentials are stored only in ignored `.tmp/stage02-local/preview-credentials.json`; they are not the hosted account.
- Final UI checkpoint: the production build, import boundaries, ESLint, TypeScript, and full Chromium suite (17/17) passed. The browser regression checks panel corner geometry, label spacing, keyboard focus, 390px overflow, sessionStorage reload persistence, and no panel preference cookies. Independent UI review found no concrete regression. One focused run timed out waiting for the dev page load event; it passed on rerun, and the test now waits for DOM content. The final full suite passed.
- Latest refactor verification: the installed React type warning is resolved. Import boundaries, ESLint, TypeScript, the production build, and the final full Chromium suite (17/17) passed. A prior full run hung during a dev-page reload because a Next dev CSS request stayed pending despite a 200 document response; the CSS asset later served normally. Removing a redundant home-page request from that browser test reduced the setup, and the focused and final full runs passed. The independent review found no concrete issue with the submit type or feature-local preference helper.
- PR #5 is under review. Its first CI run passed static/unit/build and database integration; Chromium failed because the clean job had not run Prisma client generation. The workflow now runs `corepack pnpm db:generate` after install, and the rerun passed all three jobs. Review follow-up identified a creation confirmation masking a failed refresh; a browser regression reproduced it and passed after the UI fix. The Prisma schema now matches the migration for five update actions, three updated-at defaults, and the receipt constraint name; schema validation, Prisma client generation, lint, typecheck, the production build, and 8/8 workspace Chromium tests passed locally. Next action: push the review fixes, verify the new PR checks, and complete review of PR #5 before starting another Stage 02 slice.

## Previous access checkpoint

### Implemented

- `/login` signs in existing Supabase Auth users. `/signup` requires a name and creates an account, saving its normalized value as Supabase Auth `full_name` user metadata. `/signup/verify` accepts or resends a six-digit emailed code. The code uses one accessible, paste-compatible input displayed as six cells. The forms use the existing workspace palette; neither shadcn/ui nor Lucide was added. A request-scoped `AuthHandler` contains provider operations for later sign-in methods.
- `/` requires a confirmed, non-anonymous Supabase user on the server before rendering the blank canvas. Supabase SSR cookies and the Next proxy refresh sessions; sign-out closes canvas access. One-hour HttpOnly cookies remember the pending email and code send time for the verification UI. They are not session credentials.
- The verify page shows the code's ten-minute lifetime and a 60-second resend countdown. The resend Server Action blocks early submission even when the button is bypassed. Change clears the pending address and returns to `/signup`; a direct verify visit without a pending address also returns to signup, so no arbitrary email resend form is shown. Supabase Auth independently enforces the hosted 60-second email frequency and project email rate limit. The app maps provider rate limits to useful messages. A new code can be requested after the cooldown if mail was lost; no automatic resend occurs. The verify page uses conditional delivery wording because Supabase may mask signup attempts for existing confirmed accounts.
- Local `supabase/config.toml` requires email confirmation and configures the checked-in code-only template, six-digit OTP, 600-second expiry, and 60-second email frequency. Hosted Auth was updated to the same values through the Management API, and readback confirmed them. Hosted SMTP uses Resend. Supabase manages `auth.users` and `auth.identities` in PostgreSQL; the application Prisma schema remained at Stage 01 during that checkpoint.
- Node 24.21.0, pnpm 10.34.5, Next 16.3.6, React 19.3.0, and Prisma 7.10.0 are pinned from the preceding access checkpoint. The `pnpm dev` script now binds `127.0.0.1:3100`, matching the normal developer app origin. Browser tests use an isolated app on port 3101 with `.next-e2e` output and local Supabase, so they cannot replace the hosted-backed app on port 3100. CI starts a local Supabase stack and runs browser tests. Signup and resend pass an explicit validated `/login` return URL. [Auth setup](../auth-setup.md) explains hosted deployment configuration.

### Verification

- Hosted Supabase config readback on 2026-09-24: OTP length 6, expiry 600 seconds, SMTP minimum frequency 60 seconds, code-only confirmation template, and Auth email limit 25 per hour. Resend's API marked the test-recipient signup email as delivered with a six-digit code and no confirmation link.
- A real hosted flow passed: disposable account signup in the browser, preconfirmation canvas denial, delivered code entry at `/signup/verify`, `auth.users.email_confirmed_at` set, blank canvas visible, sign-out followed by canvas denial. The disposable hosted user was deleted.
- Against local Supabase, 9/9 Chromium browser tests passed, including Change and direct-visit routing, a real resend that reached Mailpit, a forced early resend that the server rejected, wrong-code denial, delivered-code confirmation, login, sign-out, and the canvas gate. A fresh lint, typecheck, and production build passed. The unit suite passed 14/14. The integration suite passed 4/4 against a disposable bootstrapped local Supabase project. That project and its network were removed; the original local database was restored from backup and its environment identity matched the pretest value.
- Previous checkpoints also passed Prisma schema validation, frozen install, responsive 390px layout checks, and local Mailpit delivery. The earlier [01.1](../evidence/foundation/01-1.md), [01.2](../evidence/foundation/01-2.md), [01.3](../evidence/foundation/01-3.md), and [dependencies](../evidence/foundation/dependencies.md) notes remain historical.

The user-reported Gmail address was already confirmed in hosted `auth.users` before this checkpoint, and Resend listed no send to that recipient; another signup confirmation was therefore not expected. The `Failed to fetch` browser stack identified a failed Next Server Action request but contained no server-side cause. With the local Next dev server running, the Change and resend actions both completed successfully in browser tests and the server logged successful POSTs. The prior transport failure could not be reproduced. The standard `pnpm dev` command was also verified serving `/api/health` and `/signup` at port 3100.

#### Latest auth diagnosis and verification

- The reported address is confirmed in hosted `auth.users`, while a separate account with that address is unconfirmed in local Supabase. Local Mailpit captured two confirmation messages for that account. The local test app previously shared port 3100 with the normal dev app, so a browser could reach local Auth while `.env.local` pointed at hosted Auth. Test traffic now uses port 3101 and a separate build directory.
- A disposable confirmed hosted account signed into the normal port-3100 app and reached the blank canvas; sign-out restored the canvas gate. The account was removed afterward. The reported account password was not available or used.
- The signup Name field is validated before contacting Auth and persisted as normalized `full_name` metadata for new accounts. Existing accounts are unchanged. The local browser suite passed 10/10 after adding the field, including a blank-name rejection and metadata assertion; the complete signup flow passed again after the final AuthHandler refactor. Source lint, TypeScript, 14/14 unit tests, and the production build passed. The normal hosted-backed app returned HTTP 200 at `/api/health` after restart.
- Code review found that HTML `maxLength=6` truncated formatted OTP paste before the component removed nondigits. A Chromium regression reproduced `" 123456"` becoming `"12345"`; removing the redundant attribute made the same paste yield all six digits. The focused browser test, lint, and typecheck passed.
- Hosted Auth uses PostgreSQL `auth.users` and `auth.identities`. `auth.audit_log_entries` exists but contained no records when inspected; Auth events were visible in external logs. The app has no user-profile or application audit table in Prisma yet.

## Remaining deployment check

ScopeRoom has not been deployed to Vercel. Supabase's hosted Site URL is still localhost, so a production redirect cannot be verified yet. At deployment, set the exact HTTPS production origin in Supabase Site URL and allow `<production-origin>/login`; set matching Vercel public environment variables, then repeat a fresh delivered-email signup and redirect test. PR #5 is under review; verify its checks after each pushed review fix. Project data and roles belong to a later authorized stage.
